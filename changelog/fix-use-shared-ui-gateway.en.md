# Use the shared UI gateway

The meeting share button is now mounted through the central share UI gateway. Its icon, localized label, styling, guest behavior, and teardown therefore follow the established Cognis behavior.
