# Handle a hidden share trigger

The meeting page now safely handles a share trigger hidden by the share UI gateway. The page remains usable when the gateway does not provide a button for the current sharing context.
