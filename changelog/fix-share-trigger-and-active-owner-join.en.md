# Fix the share trigger and active owner join

The share trigger now uses the share UI gateway's correct container interface and is visible again. Meeting owners can also join an active meeting when only a guest using a share link is present.
